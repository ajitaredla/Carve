## Relevant Files

- `prisma/schema.prisma` - v1 data model (already written and validated)
- `prisma.config.ts` - Prisma 7 CLI connection config (already written)
- `app/layout.tsx` - Root layout, auth session provider
- `app/(dashboard)/page.tsx` - Brand home: score + single blocker
- `app/(dashboard)/assessment/new/page.tsx` - Intake form (US-01)
- `app/(dashboard)/assessment/[id]/page.tsx` - Score, waterfall, blocker view
- `app/(dashboard)/documents/[assessmentId]/page.tsx` - Generated documents view (US-05, US-06)
- `app/(dashboard)/outcomes/page.tsx` - "Log My Result" (US-15)
- `app/api/mcp/route.ts` - Carve MCP server endpoint (Streamable HTTP)
- `lib/prisma.ts` - PrismaClient + `@prisma/adapter-pg` singleton
- `lib/supabase/client.ts`, `lib/supabase/server.ts` - Supabase Auth helpers
- `lib/scoring/dimensions.ts` - 6-dimension weighted scoring (FR-01)
- `lib/scoring/dimensions.test.ts` - Unit tests for scoring
- `lib/scoring/blocker.ts` - Single-blocker selection (FR-03)
- `lib/scoring/blocker.test.ts` - Unit tests for blocker selection
- `lib/scoring/map-retailer-requirements.ts` - THE ONE shared `toScoringInput(brand, retailer)` mapping from `Retailer.requirements` JSON → `ScoringInput` (task 6.0a) — also stamps `retailerDataVersion`
- `lib/waterfall/calculator.ts` - Deterministic waterfall calculation (FR-02)
- `lib/waterfall/calculator.test.ts` - Unit tests for the waterfall calculator
- `lib/mcp/tools.ts` - MCP tool implementations (retailer requirements, waterfall, brand context, verification facts)
- `lib/agents/session.ts` - Managed Agents session-flow helper (create → stream → drain to idle → extract text)
- `lib/agents/generate.ts` - Wires carve-generator + carve-verifier into each AI surface
- `agents/carve-generator.agent.yaml` - Generator agent config
- `agents/carve-verifier.agent.yaml` - Verifier agent config
- `agents/carve-cloud.environment.yaml` - Shared cloud environment config
- `actions/assessment.ts` - Server Actions: create assessment, run scoring
- `actions/waterfall.ts` - Server Actions: waterfall calc + verdict generation
- `actions/documents.ts` - Server Actions: generate each of the 6 FR-05 document types
- `actions/outcomes.ts` - Server Actions: log PO outcome (FR-07)
- `components/assessment/`, `components/waterfall/`, `components/documents/` - UI components
- `.claude/agents/*.md` - Installed subagent definitions (nextjs-developer, typescript-pro, postgres-pro, mcp-developer, ai-engineer, ui-designer, code-reviewer, security-auditor, architect-reviewer, product-manager)
- `app/api/cron/weekly-actions/route.ts` - Weekly Action Cadence cron endpoint (FR-04)
- `lib/next-action/select.ts` - "Next specific action" selection per active brand (FR-04) — renamed from `lib/actions/` (architect review flagged a naming collision with the top-level `actions/` Server Actions directory)
- `lib/next-action/select.test.ts` - Unit tests for next-action selection
- `lib/email/weekly-action.tsx` - Resend email template for the weekly action
- `lib/auth/current-brand.ts` - `getCurrentFounderAndBrand()` / `requireCurrentBrand()` — THE shared, load-bearing brand-ownership check every Server Action must use (see file for why: Prisma bypasses RLS, so this is the real data-isolation boundary, not RLS)

### Notes

- Every parent task below runs through the four-stage gate agreed on: **implement** (named specialist subagent) → **QC** (`code-reviewer`, plus `security-auditor` where noted) → **product review** (`product-manager`, checked against the specific FR-XX/US-XX listed) → **final reviewer** (`architect-reviewer`, architecture fit + integration with completed stages).
- Unit tests colocated with the file they test (`*.test.ts`). Use `npx vitest run` (or the test runner the Next.js scaffold sets up) to run them.
- v1 scope only (PRD §12 Phases 0-4) — no Stripe, broker dashboard, Investor Readiness Test, stay-in tracking, LEAP alerts, or shelf-positioning in this task list.
- **Model policy (2026-07-25): all review gates from task 6.8 onward run on Claude Fable 5** (user has a $100 Fable credit, applied to "all remaining review gates"). This means every `code-reviewer`, `product-manager`, `architect-reviewer`, and `security-auditor` pass from 6.8 forward — builder/implementation agents (`nextjs-developer`, `typescript-pro`, `ai-engineer`, `ui-designer`, `mcp-developer`) stay on the default model. 6.7/6.7a's gates ran on default (launched before this decision).
- **Correction (caught by 1.0's product review):** the original task list omitted **FR-04 (Weekly Action Cadence)**, one of the PRD's seven v1-scope P0 requirements (§6.1). Added as task 6.5.
- **Resolved (6.6b, 2026-07-25):** one *current* assessment per brand+retailer pair (`@@unique([brandId, retailerId])` on `Assessment`) — supports multi-retailer pursuit (FR-06, US-09) via multiple rows, no unbounded per-retailer history. **Task 7.0 implication:** the dashboard/nav DOES need a way to see/switch between a brand's assessments across retailers (not just one "brand home" route) — build this into 7.1/7.2, don't assume single-retailer-only.
- **Open question logged, not blocking (from 1.0's product review):** no task in this list adds founder sign-up/registration — task 1.0's login is sign-in only. Plausible given Phase 1's concierge/manual-provisioning model (§12), but currently an implicit assumption. Revisit if it becomes a real gap.

## Instructions for Completing Tasks

As each task completes, change `- [ ]` to `- [x]` in this file — update after every sub-task, not just the parent.

## Tasks

- [x] 0.0 Create feature branch
  - [x] 0.1 Create and checkout `feature/v1-core-loop`
- [x] 1.0 Scaffold Next.js app (App Router, TypeScript, Tailwind, shadcn/ui, Prisma wiring, Supabase Auth) — `nextjs-developer`
  - [x] 1.1 Init Next.js 14+ App Router project with TypeScript + Tailwind, move existing `prisma/` and `prisma.config.ts` in (landed on Next.js 16 — flagged for architect review)
  - [x] 1.2 Install and configure shadcn/ui
  - [x] 1.3 Wire `lib/prisma.ts` (PrismaClient + `@prisma/adapter-pg`, pooled connection string)
  - [x] 1.4 Set up Supabase Auth (client + server helpers, session middleware — `proxy.ts`, Next.js 16's renamed `middleware.ts`)
  - [x] 1.5 App shell: root layout, nav, protected-route pattern for `(dashboard)`
  - [x] 1.6 QC: `code-reviewer` — found and fixed 2 open-redirect vulnerabilities in the login flow
  - [x] 1.7 Product review: `product-manager` — conditional pass; found `proxy.ts` would have blocked task 4.0's MCP bearer-auth (fixed now); logged 2 open items above
  - [x] 1.8 Final reviewer: `architect-reviewer` — conditional pass; found no Founder-identity/brand-context convention existed yet, which matters because Prisma bypasses RLS (the founder→brand ownership check is the *actual* data-isolation boundary, not RLS). Resolved: `Founder.id` = Supabase Auth user UUID (documented in schema), `lib/auth/current-brand.ts` added as the one shared helper every Server Action must use. Also flagged for 4.0/5.0: MCP tools are brand-agnostic (single shared bearer token) — ownership validation must happen in the Server Action before session kickoff, never assumed inside the MCP tools themselves.
- [x] 2.0 6-dimension scoring engine + single-blocker selection (FR-01, FR-03) — `typescript-pro`
  - [x] 2.1 Scoring input types (brand + retailer data)
  - [x] 2.2 Per-dimension scoring functions (margin 27%, distributor 23%, certification 18%, timing 13%, velocity 10%, fulfillment 9%)
  - [x] 2.3 Overall score aggregation (0-100)
  - [x] 2.4 Single-blocker selection (highest-priority gap, never two)
  - [x] 2.5 Unit tests
  - [x] 2.6 QC: `code-reviewer` — pass; added exhaustiveness guard on the blocker discriminated union, added a negative-margin edge-case test, fixed a mislabeled test. Flagged 2 tuning/design calls for product review: the margin surplus-cap/deficit-floor constants, and the "headroom cost" blocker-selection formula (PRD gives only one worked example, doesn't specify the formula)
  - [x] 2.7 Product review: `product-manager` — pass, both flagged design calls upheld: margin surplus cap is correctly scoped to point-in-time readiness (not durability, which is FR-02/stay-in's job); headroom-cost blocker selection is correct over raw score (raw score would systematically over-surface Timing, which is often unfixable data-unavailability, not an actionable gap — undermines US-04)
  - [x] 2.8 Final reviewer: `architect-reviewer` — conditional pass; `lib/scoring/` integrates cleanly, no changes needed to this task's code. One concrete follow-up tracked into 6.0 below (shared `toScoringInput` mapping, to avoid it being built twice), plus watch-items added to 4.0/6.0 (error handling at the request boundary, `retailerDataVersion` stamping, Assessment+CostWaterfall transaction wrap)
- [x] 3.0 Cost waterfall calculator (FR-02) — `typescript-pro`
  - [x] 3.1 Waterfall input/output types
  - [x] 3.2 Deterministic calculation (factory cost → MSRP flow)
  - [x] 3.3 Investor verdict thresholds (pass/marginal/fail)
  - [x] 3.4 Unit tests
  - [x] 3.5 QC: `code-reviewer` — pass; fixed a missing lower-bound validation on `retailerMarginPct` (negative values would invert the money-flow ordering invariant), stress-tested currency precision (195k samples, max drift 1 part in 10,000 — fine). Flagged for product review (3.6): should `distributorMarkupPct` allow negative values (implied discount/rebate)? Flagged for task 7.3 (waterfall UI): `moneyFlow`'s "founder gross margin" step excludes `chargebackEstimate` by design while `economics.founderGrossProfitPerUnit` includes it — these will show different numbers, document the distinction in the UI
  - [x] 3.6 Product review: `product-manager` — pass; ratified the 50/30 verdict thresholds as correctly calibrated to §2.2's O'Leary "thin margins" framing (not too lenient); ruled `distributorMarkupPct` should NOT allow negative values (no real distributor contract has a negative markup — rebates belong in `chargebackEstimate`) and fixed the validation floor to 0
  - [x] 3.7 Final reviewer: `architect-reviewer` — conditional pass; `lib/waterfall/` needs no changes. Four integration notes carried forward to 4.3/4.7, 6.0a/6.3, 6.6a, and 7.3 below.
- [x] 4.0 Carve MCP server, 4 read-only tools — `mcp-developer`
  - [x] 4.1 Scaffold `/api/mcp` Streamable HTTP route
  - [x] 4.2 `get_retailer_requirements` tool
  - [x] 4.3 `run_waterfall_calculator` tool (reuses 3.0 logic) — write a zod input schema (`calculateWaterfall` only exports a TS interface, not a runtime schema); wrap the call in try/catch against a typed error (see 4.3a below, not a generic `Error` string-match); **the verifier calls this tool to re-confirm the exact inputs the Server Action already computed and persisted — it must NOT let the agent reconstruct/re-derive its own input values** (architect review's #4: source-of-truth must stay with the Server Action, or the AI-narrated verdict can cite numbers that don't match what's in `CostWaterfall`)
  - [x] 4.3a Add a typed error (e.g. `class WaterfallInputError extends Error`) in `lib/waterfall/calculator.ts` so tool handlers / Server Actions can branch on validation failures without string-matching `error.message`
  - [x] 4.4 `get_brand_context` tool
  - [x] 4.5 `get_verification_facts` tool
  - [x] 4.6 Bearer-token auth middleware on the endpoint
  - [x] 4.7 QC: `code-reviewer` + `security-auditor` — both pass. code-reviewer found and fixed a real bug: `.strict()` Zod schemas were dead code (SDK was passed `.shape` instead of the schema, silently accepting extra fields — confirmed live before/after). security-auditor confirmed the auth check and injection safety are sound, added a minimal audit-trail log on the two brand-data tools (single point of failure for cross-brand isolation, no way to detect misuse without it), and fixed a real integrity gap (`investorVerdict` was cast from an unconstrained DB string with no runtime check — now validated, fails closed). Flagged for architect review: 2 tools missing `outputSchema`; real rate limiting needs shared state (Redis/Upstash) not yet in the stack.
  - [x] 4.8 Product review: `product-manager` — conditional pass, one real gap fixed before continuing: `get_verification_facts` had no `brandId` at all, so a mismatched brandId/assessmentId pair (upstream bug, stale id) would silently pull a real but different brand's data and generate confident, plausibly-cited content — worse than "inventing" a fact because it looks legitimate. Fixed: added optional `brandId` param (validated like the existing `costWaterfallId` mismatch check) and always return the assessment's real `brandId` in the response. Also corrected the tool's description, which overclaimed self-sufficiency — verifying retailer-specific facts (not just normalized scores) requires ALSO calling `get_retailer_requirements`; flagged explicitly for 5.0's verifier agent config. One item logged as backlog, correctly out of v1 scope: `Retailer.requirements` has no history/versioning table, so §10.1's "reconstruct which retailer-data snapshot produced a document" can't fully be honored once a retailer is rescraped — belongs to a future retailer-ingestion task, not yet in this list.
  - [x] 4.9 Final reviewer: `architect-reviewer` — conditional pass, task 4.0 done. Ruled: add `outputSchema` to the 2 tools missing it (done — highest-scrutiny tools, had the least validation, inverted risk profile). Fixed a stale row in `.scratch/carve-v1-agentic-architecture.md`'s tool table (said "assessmentId or costWaterfallId" as alternate lookup keys — actually assessmentId is always required, costWaterfallId is an optional cross-check). Two notes carried to 5.0/6.0 below.
- [x] 5.0 Managed Agents setup (carve-generator, carve-verifier, environment, vault) — `ai-engineer`
  - [x] 5.1 `carve-generator.agent.yaml` — written; created live as `agent_01MfbJwysr8k5C8su7f5ayuC` (v2), confirmed by GET.
  - [x] 5.2 `carve-verifier.agent.yaml` — written with the both-tools + brandId rules from 4.8's review baked into the system prompt; created live as `agent_01NrpxcJKSqy6RQhCuyUoRuz` (v2), confirmed by GET.
  - [x] 5.3 `carve-cloud.environment.yaml` — written; created live as `env_01KdjpSorViYqZAh22pZTZkV` (type cloud, unrestricted networking), confirmed by GET. **Superseded in 5.6**: networking tightened to `limited` + `allow_mcp_servers: true` by the security-auditor QC pass — see 5.6.
  - [x] 5.4 Vault + MCP credential — **credential type resolved: `static_bearer` is correct** (NOT `mcp_oauth`, which is for OAuth-with-refresh servers, and NOT `environment_variable`, which is for non-MCP CLI/SDK/`curl` secrets substituted at egress). Carve's `/api/mcp` uses a single static `Authorization: Bearer` token, and Managed Agents injects it into the MCP connection via a vault credential keyed by `mcp_server_url` — exactly what `static_bearer` is for. The architecture doc's original assumption was right. Created vault `vlt_011CdPCJPHimrJC1sRHaWHYr` + credential `vcrd_01Dp1F3Lf8MWUnEmDoP4uRbL` (holds `.env`'s `MCP_SERVER_TOKEN`); `token` is write-only (never echoed back), confirmed by GET. (`ant` CLI not installed here — used raw `curl`/API directly.)
  - [x] 5.5 Applied via raw `curl`/API (not `ant` — not installed): environment + both agents created, IDs written to `.env` and a new `.env.example` section, each confirmed by GET rather than trusting the create response. **Found + fixed:** the API defaults an `mcp_toolset` to `permission_policy: always_ask`, which would deadlock the autonomous drain-to-idle Server Action flow (task 6.0) — set both agents to `always_allow` (safe: all four tools are read-only), bumping them to v2. **Deferred (blocked on deploy):** the empirical stateless-MCP-session question (does the MCP client hold one connection across tool calls vs. reconnect per call, against this server's `sessionIdGenerator: undefined` stateless design) cannot be answered until `/api/mcp` is a public HTTPS URL — `localhost` is unreachable from Anthropic's infra and no deploy task exists yet. Post-deploy test: update the placeholder `url` in both agent YAMLs and the vault credential's `mcp_server_url` to the real origin, then create one session and have it call two different Carve tools (e.g. `get_brand_context` then `run_waterfall_calculator`); if the second fails on a lost session, switch the route to session-ID mode (cheap — no real state to preserve).
  - [x] 5.6 QC: `code-reviewer` + `security-auditor`
    - `code-reviewer` (quality half): general quality/prompt-correctness pass, plus flagged the `carve-cloud` environment's `unrestricted` networking as a standing shared-environment risk (a future agent placed there with `bash`/`web_fetch` would silently inherit unrestricted egress) — left as a security/product judgment call rather than resolved.
    - `security-auditor` (security half, this pass): **PASS**, one live fix applied. Independently verified against the live API (not just the task file's claims): (1) `GET /v1/vaults/{id}/credentials` confirmed write-only — the `static_bearer` credential's `auth` object returns only `mcp_server_url`/`type`, never the token value; (2) grepped the full repo (tracked + untracked, excluding `.env`) for the real `MCP_SERVER_TOKEN` value — no match anywhere, including both agent YAMLs (placeholder URLs only, no embedded token); (3) `.env` confirmed still `git check-ignore`d, never appears in `git status`/`git log`, nothing staged. **Fixed live:** `carve-cloud` environment's networking was `unrestricted` — tightened to `type: limited, allow_mcp_servers: true` via the API (`POST` to the environment resource is the update verb here, not `PATCH`/`PUT`, both of which 405), re-verified by GET, and the YAML updated to match. Rationale: zero functional downside today (both agents only use `mcp_toolset`, which doesn't consume this networking setting anyway) and it closes the standing-grant gap the code-reviewer flagged — a future agent added to this shared environment no longer inherits unrestricted egress by default. **`always_allow` on both agents' MCP tools**: not a concern beyond what task 4.0 already accepted — the single-shared-bearer-token/no-per-founder-identity trust model means auto-allowing 4 read-only tools adds no new authorization surface; brand-ownership checks already happen upstream in the Server Action before session creation, same as before. **Blast radius, reconfirmed now that this is live (not just designed):** if `CARVE_VAULT_ID`'s credential or `.env` were compromised, an attacker gets bearer-token access to the 4 read-only Carve MCP tools — read access to retailer requirements, brand context, and verification facts across *all* brands (no per-founder scoping at this layer, by design), plus use of the pure-compute waterfall calculator. No write path exists through these tools. This matches task 4.0's accepted trust model exactly; going live doesn't change the calculus, it just means the credential now has real access to revoke if this vault is ever suspected compromised (`vlt_011CdPCJPHimrJC1sRHaWHYr` / credential `vcrd_01Dp1F3Lf8MWUnEmDoP4uRbL`). **Flagged, not blocking:** no credential-rotation runbook exists yet for this vault — worth a short note somewhere (§15.2-style open question) before production traffic, since today rotation would mean manually replacing both the vault credential and `.env`'s `MCP_SERVER_TOKEN` with no documented procedure.
  - [x] 5.7 Product review: `product-manager` — conditional pass; role separation (generator writes, verifier independently re-checks, neither does both) is correct per §10.1. Three real findings: (a) §7's exact retry state machine (FLAGGED → regenerate once → re-verify → PASS=final or FLAGGED-again="needs review") isn't precisely specified anywhere for 6.0's implementer — added as explicit subtask 6.1a below; (b) both agents use `claude-haiku-4-5`, which silently contradicts PRD §10's architecture table (still says `claude-sonnet-4-6`) — this was the user's own explicit "cheapest tier" instruction earlier in this project, not a mistake, but it was undocumented as a deviation; reconciled directly in the PRD (see Document History v3.2 below) rather than left as only a YAML comment. Flagged as a real risk: the verifier is more load-bearing than the generator (a missed verification error becomes a silent false PASS, worse than no verification), so symmetric cheapest-tier deserves an explicit eval pass before Phase 3 ships all 6 document types, not just a default; (c) §10.2's cost table was built on Sonnet pricing and Managed Agents session/container overhead isn't modeled at all — corrected in the PRD, flagged for whoever monitors production spend.
  - [x] 5.8 Final reviewer: `architect-reviewer` — **PASS** for 5.0's own deliverables (agents/environment/vault correctly built and live-verified, no changes needed). Four integration risks for 6.0 identified and incorporated below (6.0b deploy task added; 6.1's helper return shape specified; 6.1a corrected to pass the verifier's discrepancy forward; 6.4/7.4 concurrency flagged).
- [x] 6.0 Document generation (FR-05) + PO outcome logging (FR-07) — `ai-engineer` + `nextjs-developer`
  - [x] 6.0a **`lib/scoring/map-retailer-requirements.ts`** — the ONE shared `toScoringInput(brand, retailer): ScoringInput` function (per 2.0's architect review — do not let task 4.0's MCP tool and this task's Server Action each build their own mapping from `Retailer.requirements` JSON). This is also where `retailerDataVersion` gets stamped (from `Retailer.updatedAt`) onto both `Assessment` and `GenerationLog`. Wrap the `scoreDimensions`/`scoreMarginReadiness` call site's known throw (invalid `retailPrice`) in a try/catch here or at the call site, turned into a structured validation error — not an unhandled 500. **Schema gap found and fixed (2026-07-25, main thread):** the builder correctly discovered Prisma's `Brand` model had nowhere to persist the founder-entered facts scoring needs (wholesale/retail price, distributor relationships, EDI/EFT, certs held, DTC/velocity, fulfillment facts) and, rather than unilaterally inventing columns, flagged it and took a `BrandScoringFacts` parameter instead of literal `Brand`. Resolved directly: added all these fields to `Brand` in `prisma/schema.prisma` (plus a new `Certification` enum matching `CertificationType`'s string union), re-validated (`prisma validate`/`generate` pass), and simplified `toScoringInput` to take Prisma's real `Brand` type (`BrandScoringFacts` is now just a documented `Pick<Brand, ...>` alias, with `.toNumber()` conversions for the `Decimal` fields). This is what task 7.1's intake form will read from/write to — it now has a real persistence home. All 72 tests still pass after the change.
  - [ ] 6.0b **DEPLOYMENT — owned by the user, not this build pipeline (decided 2026-07-25).** Deploying Carve to a public HTTPS endpoint (needed so Anthropic's Managed Agents can reach `/api/mcp` — both agent YAMLs and the vault credential still point at the placeholder `mcp_server_url`) is being done by the user directly on **Azure**, not Vercel, and not by an agent in this pipeline. Do not attempt to deploy anything as part of this task list. Once deployed, the user needs to: (1) update both agents' `mcp_servers[0].url` (via the API's update verb — `POST` to the agent resource, not `PATCH`/`PUT`, both 405 per 5.6's finding) and the vault credential's `mcp_server_url` to the real origin, (2) run 5.5's deferred empirical test (does the Managed Agents MCP client hold one session across tool calls, or reconnect per call, against this server's stateless design). Everything else in 6.0-6.6 can and should be built and unit-tested against mocked `sessions.create`/`events.stream`/`events.send` without waiting on this — only real (non-mocked) end-to-end session testing is blocked on it.
  - [x] 6.1 Session-flow helper (create → stream → drain to idle → extract text). Per 5.8's architect review: (a) `vault_ids` must be passed at `sessions.create()` time — it cannot be attached via session update (unsupported by the API); (b) drain-loop gate is NOT `status === 'idle'` — sessions go transiently idle between parallel tool calls too. Break on `session.status_terminated`, or `session.status_idle` where `stop_reason.type !== 'requires_action'`; (c) treat a `session.error` stream event as informational (log it), not as the call's outcome — a failed tool call surfaces as the agent's own text (e.g. the verifier legitimately responding "could not verify"), not a thrown exception, so don't assume session-created-successfully means every tool call inside it succeeded; (d) open the event stream before sending the kickoff `user.message` (stream-first ordering) or early events can be missed. **Return shape** (needed by 6.5's `GenerationLog` persistence — bake this in now, retrofitting after 6.2-6.4 are written against a narrower shape is more expensive): `runGeneratorSession(prompt): Promise<{text, sessionId, usage}>` and `runVerifierSession(prompt): Promise<{result: 'PASS' | {flagged: string}, sessionId, usage}>` — not bare `Promise<string>`. A session-level failure (`retries_exhausted`, `terminated`, stream error) should throw/reject, distinct from a normal `FLAGGED` return value.
  - [x] 6.1a **Retry/review-state machine (per §7 Verification NFR, precisely specified here per 5.7's product review, corrected by 5.8's architect review):** on a FLAGGED verification result, do NOT show an error and do NOT persist the content. Instead: (1) regenerate once, passing the verifier's exact `FLAGGED: <discrepancy>` text into the regeneration prompt so the model has something concrete to correct — a fresh kickoff with identical inputs and no feedback has a real chance of reproducing the same flagged content, since the generator's tools are deterministic; (2) **decision made in 6.1: continue the original generator session** (not a new one) via `sendFollowUp(sessionId, message)`, already built in `lib/agents/session.ts` — cheaper (3 session lifecycles for flagged-then-fixed vs. 4) and gives the model the actual discrepancy to correct against; this subtask just needs to call it, not decide it; (3) re-verify the new output; (4) if PASS, persist and show as final (founder never sees the first attempt existed); (5) if FLAGGED again, do NOT retry a third time — persist nothing and surface an explicit "this result needs review" state (distinct from a normal error — this is a known, named state per §7, not a failure to handle gracefully). This state machine applies identically to every generation surface: 6.2 (blocker), 6.3 (waterfall verdict), and each of the 6 document types in 6.4.
  - [x] 6.1b **Verifier reliability check (per 5.7's product review, before 6.4 ships all 6 document types):** since a verifier error becomes a silent false PASS (worse than no verification — it manufactures false confidence, per 5.7's finding that the verifier is more load-bearing than the generator), run a small eval batch of known-good vs. known-bad generations through the Haiku carve-verifier before considering 6.4 done. If it misses known-bad cases at a meaningful rate, escalate the verifier (not the generator) to a stronger model — this is a targeted, verifier-only upgrade if needed, not a reason to change the generator's model choice.
  - [x] 6.1c **Document-generation concurrency (per 5.8's architect review — unresolved open item from the original architecture doc, now concrete):** decide explicitly whether 6.4's 6 document types generate in parallel (`Promise.all` across independent documents) or sequentially. Each document's happy path is 2 session lifecycles (generate + verify); the flagged path is up to 4 (see 6.1a) — a fully sequential run across all 6 documents could mean up to 24 session lifecycles in one request, a real risk of exceeding Vercel serverless function timeouts, not just a UX nicety. This decision touches 6.4 AND task 7.4's UI (which must be built for async/progressive results if generation is parallelized, not a single blocking response) — decide here, before 7.4 is built assuming the wrong shape.
  - [x] 6.2 Blocker statement generation + verification (FR-03)
  - [x] 6.3 Waterfall verdict generation + verification (FR-02) — per 3.7's architect review: `WaterfallInput.retailerMarginPct` comes from the same `Retailer.requirements` JSON `toScoringInput` (6.0a) already parses — get it from `toScoringInput(brand, retailer).margin.retailerMinGrossMarginPct` rather than re-deriving it independently in `actions/waterfall.ts`. Everything else in `WaterfallInput` (factory cost, co-packing fee, freight, chargeback, MSRP) is founder-entered intake-form data needing no mapping step. **Per 4.9's architect review**: `actions/waterfall.ts` computes+persists `CostWaterfall` by importing `calculateWaterfall` directly from `lib/waterfall/calculator.ts` (in-process — no MCP round-trip for the deterministic math itself). The MCP tool `run_waterfall_calculator` is a *separate* thing, only ever called over HTTP by the `carve-verifier` Managed Agents session to independently re-confirm those same persisted inputs — don't try to import a "run_waterfall_calculator" function (nothing by that name is exported from `lib/mcp/tools.ts`), and don't have the Server Action call its own `/api/mcp` endpoint over HTTP (circular, needs its own bearer token, no benefit).
  - [x] 6.4 Each of the 6 FR-05 document types (KeHE application, Sprouts checklist, WF pitch brief, sell sheet outline, UNFI application, buyer outreach email)
  - [x] 6.5 `GenerationLog` persistence for every generation + verification call
  - [x] 6.6 "Log My Result" outcome logging (FR-07)
  - [x] 6.6a `Assessment` + `CostWaterfall` creation wrapped in a single Prisma transaction (`prisma.$transaction`) — avoid a scored-but-no-waterfall partial write if the waterfall calc throws after the assessment is already created
  - [x] 6.6b **Schema fix + open product question resolved (2026-07-25, main thread):** the builder correctly flagged that `Assessment` had no `@@unique([brandId, retailerId])` constraint, so `lib/assessment/persist.ts`'s one-row-per-pair invariant was enforced at the application level only (`findFirst` then `create`/`update`) — a real race condition under a concurrent double-submit. Fixed: added the compound unique constraint to `prisma/schema.prisma`, converted `persist.ts` to a real atomic `db.assessment.upsert(...)`. **This also resolves task 1.0's long-open product question** ("does v1 support only one active assessment per brand at a time, or does multi-retailer pursuit need more?"): the answer is one *current* assessment per brand+retailer pair — supports multi-retailer pursuit (FR-06, US-09) via multiple rows keyed by `retailerId`, without an unbounded history per retailer (re-scoring upserts in place). Updated `lib/assessment/persist.test.ts`'s mocks accordingly (`findFirst`/`create`/`update` → `findUnique`/`upsert`). All 109 tests still pass.
  - [x] 6.7 QC: `code-reviewer` — pass, no fixes needed (code was already clean). Confirmed: ownership checks (`requireCurrentBrand()`) present and first in every `actions/*.ts` file; retry state machine matches 6.1a's contract exactly (traced line-by-line); GenerationLog rows correct for every normal PASS/FLAGGED path; 6.6a's transaction correctly uses `tx` throughout, rolls back on `WaterfallInputError`; 6.4's parallel generation correctly isolates failures (never rejects `Promise.all`); the main-thread schema patch (6.6b) confirmed correct and complete, not half-applied. One real gap flagged (not fixed — a design-scope tradeoff, not a bug): session-level failures (network drop, `retries_exhausted`) produce ZERO `GenerationLog` rows — `generate.ts` deliberately excludes them from the state machine to stay unit-testable without a DB mock, but a failed attempt still costs tokens and is invisible to what the schema calls "the full provenance trail." Carrying to 6.8/6.9 for a product/architecture call. Given the real ownership-check surface here (data isolation across 4 Server Actions, same class as tasks 4.0/5.0), adding a `security-auditor` pass too, consistent with how this pipeline treated similar surfaces.
  - [x] 6.7a Security review: `security-auditor` (added given the ownership-check/data-isolation surface across `actions/*.ts`) — first attempt hit a credit-balance error mid-fix; left `wrapUntrustedField` (a real, well-reasoned prompt-injection mitigation: founder-controlled `Brand.name`/`Brand.category` are embedded verbatim into AI kickoff prompts alongside authorized `brandId`/`assessmentId` values, so an adversarial brand name could attempt to redirect the agent's own MCP tool calls to a different id — defense-in-depth, not a substitute for real MCP-layer auth) wired into `actions/assessment.ts` but not yet into `actions/waterfall.ts`/`actions/documents.ts`, breaking 5 tests. **Completed in the main thread**: applied the identical wrap-and-reinforce pattern to both remaining files' kickoff prompts (`buildVerdictKickoffPrompt`, `documents.ts`'s shared `sharedContextBlock` — covers all 6 document types via one function), fixed the 3 test files' module mocks to pass the real `wrapUntrustedField` through via vitest's `importOriginal` (not a hand-copied reimplementation, which would silently drift). All 109 tests pass, clean typecheck/lint. **Re-run with the real `security-auditor` agent type** (harness restarted mid-task, real subagent types became available) — **PASS**, confirmed the fix is complete and correct everywhere, including the 4 verify-prompt builders (no gap there — they only interpolate trusted server-derived values + the pre-existing AI-text sandwich). IDOR, `brandInputSnapshot` PII, and `outcomes.ts` validation all confirmed safe. Two items surfaced, not fixed, carried to 6.8 for an explicit product ruling: **(a)** the new `@@unique` constraint prevents duplicate Assessment/CostWaterfall *rows* but NOT duplicate, wasteful `generateWithVerification` calls — `generateWithVerification` runs AFTER the upsert transaction commits, so two concurrent requests for the same brand+retailer both proceed to expensive AI generation, with a last-write-wins race on the final `blockerStatement`/`verdictStatement`. Not a security issue (same authenticated founder, no cross-tenant leak) — a cost/data-integrity tradeoff: accept as a narrow, low-frequency risk for v1, or add a lock/dedup mechanism? **(b)** `actions/documents.ts`'s `runOneDocument` returns raw `error.message` to the caller (explicit, tested behavior in `documents.test.ts`) — inconsistent with the MCP layer's `safeToolCall` sanitization precedent, though today's actual content at risk is low-sensitivity. Independently re-verified by the main thread: `npx tsc --noEmit`/`eslint`/`vitest run` all still pass (109/109) — the security-auditor's own toolset was read-only this session and couldn't confirm this itself.
  - [x] 6.8 Product review: `product-manager` (Fable 5) — conditional pass, one must-fix bug found and fixed, plus explicit rulings on the 2 items carried from 6.7a:
    - **RULE (item 1, concurrent duplicate generation): accept as-is for v1.** No cross-tenant exposure, both branches work from identical committed facts, cost is trivial at Haiku pricing, and PRD §12's concierge-scale gates (Phase 1: 3 paying customers) make a determined double-submit rare. Only sanctioned mitigation: 7.0's UI disables the button on click — do NOT build a server-side lock/dedup mechanism now. Revisit only if real production traffic ever makes it a real problem (Phase 9 territory).
    - **RULE (item 2, raw error messages): the "show founders the real reason" instinct is right, but the current blanket `error.message` passthrough isn't that — it needs triage, not a binary sanitize/don't-sanitize call.** Known operational failures (`AgentSessionError`) should get a short, friendly, actionable message; genuinely unexpected internals (Prisma errors, missing env config — confirmed these CAN leak ops-internal text like "Copy .env.example...") should be sanitized + server-logged like the MCP layer. **Required before 7.4 ships** (not a 6.0 blocker) — tracked as new subtask 7.4a below.
    - **Own finding (bug, MUST-FIX, why this wasn't a clean pass): `sprouts_checklist` and `wf_pitch_brief` hardcoded literal retailer names** ("Sprouts Farmers Market", "Whole Foods") in the document-specific instruction, ignoring which retailer the assessment actually targets — while `sharedContextBlock` in the SAME prompt correctly states the real `ctx.retailer.name`. A Sprouts-assessed founder could receive a document titled/instructed as "Whole Foods," backed by Sprouts's verified economics — either wastes a generation cycle on an avoidable `needs_review` (verifier catches the contradiction) or worse, ships a factually wrong document. **Fixed** (main thread): both cases now use `${ctx.retailer.name}` like `buyer_outreach_email` already correctly did; also tightened the Sprouts checklist prompt to ask for direct links (FR-05's table promises "Includes direct links," the old prompt only asked for "next-step instructions"). No test assertions depended on the old hardcoded text.
    - **Own finding (gap, fixed): `actions/outcomes.ts`'s `assessmentId` link was optional with no auto-resolution**, weakening FR-07's "against that brand's assessment history" traceability promise for any future caller (7.x UI) that simply forgets to pass it — even though a deterministic mapping already exists via `Assessment`'s `@@unique([brandId, retailerId])`. **Fixed**: `logOutcome` now auto-resolves the current assessment for the brand+retailer pair when `assessmentId` isn't explicitly supplied; stays genuinely `null` only when no assessment has ever been run for that pair. Added a new test for the auto-resolve path (110 tests total now, up from 109). Caveat noted, not solved: because `Assessment` is upserted in place, even a correctly-linked `assessmentId` points to the row's CURRENT state, not a point-in-time snapshot of what it said when the outcome was logged (unlike `GenerationLog`) — a known v1 tradeoff, not a new problem.
    - Document-prompt depth otherwise judged adequate (each instructs the agent to call `get_brand_context`/`get_retailer_requirements` for grounding, and 6.0a's `Brand` schema fix means the facts these tools need actually exist now).
  - [x] 6.9 Final reviewer: `architect-reviewer` (Fable 5) — **PASS, task 6.0 closes here.** No code changes needed in 6.0's own scope — AI/deterministic separation, transaction boundaries, ownership checks, and the retry state machine are all correctly and consistently implemented across all four surfaces. Five concrete forward-looking risks identified for 7.0, incorporated into that section below: (1) `needs_review` state isn't durably reconstructible on a page reload — `Assessment.blockerStatement`/`CostWaterfall.verdictStatement` fall back to a stale/pending value indistinguishable from "never attempted," only `GenerationLog.output` actually preserves the discrepancy text; (2) 7.4a's error-triage fix only covers `documents.ts` — `assessment.ts`/`waterfall.ts` have the identical raw-message-leak risk, just via a thrown exception instead of a returned `error` state; (3) `Brand` schema has real, concrete intake-form gaps: no certification label-mapping helper, no `leadTimeDays` bounds, `wholesalePrice`/`retailPrice` are completely unvalidated (a zero/negative wholesale price would silently flow into scoring — only 7.1's form can catch this), no UI affordance planned for the `isDtcOnly`/`unitsPerStorePerWeek` conditional relationship; (4) no mock seam exists anywhere for testing AI-dependent flows without a live deployment — recommends a `CARVE_MOCK_AGENTS=1`-gated fake session layer before 7.4 is built, so 7.6a isn't entirely gated on the user's Azure deployment for 3 of its 7 golden-path steps; (5) `generateBlockerStatement`'s `needs_review` result omits `overallScore` even though it's deterministic and already computed, inconsistent with `generateWaterfallVerdict`'s `needs_review` shape which does include its deterministic fields.
- [ ] 6.5 Weekly Action Cadence (FR-04) — `typescript-pro` + `nextjs-developer` — **Built after 7.0 (2026-07-25), per an explicit user decision at 7.9's architect review, which found this P0 requirement (PRD §6.1: "the seven requirements below are the v1 build") was never implemented in the original build sequence — no cron endpoint, no email template, no next-action logic existed anywhere.** PRD FR-04 text: "Every Monday, Carve sends one specific action per active user. Named. Time-bound. Includes a draft email or template where relevant. Completing an action updates the readiness score and unlocks the next action." Email-only delivery — no new dashboard UI screen; the "next action" arrives via email, consistent with the PRD's own description.
  - [x] 6.5.1 "Next specific action" selection logic (`lib/next-action/select.ts`, per the `Relevant Files` list) — reuses the shared scoring/blocker engine and recomputes a named, time-bound action from current brand facts.
  - [x] 6.5.2 Weekly cron endpoint (`app/api/cron/weekly-actions/route.ts`) — iterates assessed brands, recomputes their action, and rejects unauthenticated requests using a constant-time `CRON_SECRET` bearer-token comparison.
  - [x] 6.5.3 Resend email template + send integration (`lib/email/weekly-action.ts`) — delivery is behind a testable mailer interface; tests mock Resend and make no live network calls.
  - [x] 6.5.4 Unit tests — cover all six blocker dimensions, authenticated cron delivery, and mocked Resend success/failure behavior.
  - [ ] 6.5.5 QC: `code-reviewer` + `security-auditor` (added given the new public cron endpoint — same class of risk as `/api/mcp`, warrants the same dedicated security pass)
  - [ ] 6.5.6 Product review: `product-manager` (against FR-04 — confirm the generated action text is genuinely "named, time-bound," not generic)
  - [ ] 6.5.7 Final reviewer: `architect-reviewer` (confirm integration with the existing scoring/blocker engine, no duplicated logic)
- [x] 7.0 UI: intake, dashboard, blocker, waterfall, documents, outcomes — `ui-designer` + `nextjs-developer` — **Design direction: EXACT curious.pm theme, end-to-end across all views (user requirement, 2026-07-25).** Real tokens extracted from curious.pm's live compiled CSS (see `.scratch/curious-pm-design-tokens.md` — supersedes an earlier, incorrect qualitative note that wrongly said "dark charcoal/emerald green"; verified ground truth is a warm cream palette):
    - `--background:#fffbf3` (warm cream, NOT dark mode), `--foreground:#203208` (deep olive-black text), `--border:#1a1a1a` (near-black high-contrast borders), `--ring:#1a1a1a`
    - `--accent:#fbd072` (golden-amber, used sparingly — one highlight at a time, not a wash), `--accent-foreground:#1f3a0a`, `--primary:#fde3aa` (pale gold), `--muted:#f3f0e3`, `--muted-foreground:#3b4a2a`, `--input:#e5e2d6`, `--destructive:#e63946`
    - `--radius:24px` (very rounded cards/buttons)
    - Fonts: **Clash Display** (section/hero headings), **Clash Grotesk** (subheadings), **Inter** (body/UI text), **Geist Mono** (numeric/currency figures — waterfall line items, scores; monospace for column-aligned numbers)
    - Confirm Clash Display/Grotesk licensing (Fontshare) before bundling.
    - Apply these exact tokens as shadcn/ui theme variables (`app/globals.css`) across every 7.1-7.6 view — this is the literal palette/type system, not a loose inspiration. Adapt only where curious.pm's marketing-site patterns (illustration, long-form copy) don't fit a data-heavy dashboard — legibility of scores/dollar figures takes priority over decorative flourish where they'd conflict, per the tokens doc's guidance.
  - [x] 7.0a **Mock session seam for `lib/agents/session.ts` (per 6.9's architect review — build BEFORE 7.4, so 7.6a isn't entirely gated on the user's Azure deployment).** No mock/test seam exists anywhere for exercising AI-dependent flows without a live Managed Agents connection — `getClient()` unconditionally constructs a real `Anthropic` client. Add a `CARVE_MOCK_AGENTS=1`-gated fake implementation of `runGeneratorSession`/`runVerifierSession`/`sendFollowUp` returning canned `final`/`FLAGGED`/thrown-`AgentSessionError` responses, so 7.2/7.3/7.4's UI can be built and 7.6a can exercise all three result states (final/needs_review/error) deterministically pre-deployment.
  - [x] 7.0b **`needs_review` durable-reconstruction design decision (per 6.9's architect review — decide before 7.2/7.3/7.4, not mid-build).** `Assessment.blockerStatement`/`CostWaterfall.verdictStatement` fall back to a stale-or-pending value on `needs_review` — indistinguishable, from a normal query, from "generation never attempted." Only `GenerationLog.output`/`verificationResult` actually preserves the discrepancy text and outcome, with no documented helper for reconstructing "this assessment currently needs review" on a fresh page load (as opposed to reacting to the Server Action's live return value in the same request that triggered it). Decide explicitly: query the latest `GenerationLog` row per surface on page load or a dedicated status column? A founder who triggers generation, sees a `needs_review` toast, then refreshes must not silently lose that state.
  - [x] 7.1 Intake/onboarding form (US-01) — **`Brand` schema validation gaps for this form specifically (per 6.9's architect review), none caught by any other layer:** `wholesalePrice`/`retailPrice` are completely unvalidated everywhere in the codebase except `retailPrice`'s own `&lt;= 0` guard inside `scoreMarginReadiness` — `wholesalePrice &lt;= 0` has NO guard anywhere and would silently flow into scoring; this form is the only place that can catch it. `leadTimeDays: Int` has no min/max anywhere — add sensible bounds (e.g. 0-365). `heldCertifications: Certification[]` needs a hand-built snake_case-to-display-label map (no such helper exists yet) for its multi-select. `unitsPerStorePerWeek` is conditionally meaningful on `isDtcOnly` — `toScoringInput` silently drops it when `isDtcOnly` is true rather than rejecting a populated-but-ignored value, so the form needs its own show/hide or disable affordance, not just rely on the mapper.
  - [x] 7.2 Dashboard: score + single blocker (US-01, US-04) — per 6.9's architect review: `generateBlockerStatement`'s `needs_review` result does NOT include `overallScore` (even though it's deterministic and already computed) — don't assume it's there the way `generateWaterfallVerdict`'s `needs_review` shape includes its own deterministic fields; fetch it separately if needed on that path. Also: `assessment.ts`'s known-error paths (`ScoringInputMappingError`, "no retailer found", session failures) THROW rather than returning an `error` state like `documents.ts` does — 7.4a's error-triage fix does NOT cover this file; this view needs its own try/catch + friendly-message triage around `generateBlockerStatement`.
  - [x] 7.3 Waterfall calculator view (US-02) — per 3.5's QC flag: `moneyFlow`'s "founder gross margin" step excludes `chargebackEstimate`, `economics.founderGrossProfitPerUnit` includes it; if shown side-by-side, label the distinction so founders don't see two contradictory "profit" numbers. Also per 3.7's architect review: `CostWaterfall` only persists the 7 scalar inputs + `founderMarginPct` — there is no column for `moneyFlow`/`economics`, so this view must call `calculateWaterfall()` again on the stored scalars to re-derive them for rendering (recompute-on-read is the intended design, not a gap). Per 6.9's architect review: `waterfall.ts`'s known-error paths also THROW rather than returning an `error` state (same gap as 7.2's note above, not covered by 7.4a) — needs its own try/catch + friendly-message triage around `generateWaterfallVerdict`.
  - [x] 7.4 Document generation views (US-05, US-06) — `app/(dashboard)/assessment/[id]/documents/page.tsx` + `components/documents/documents-board.tsx`/`document-card.tsx`. Per 6.1c's concurrency decision, renders all 6 FR-05 document types as 6 INDEPENDENT cards, each with its own final (copy-to-clipboard + readable content)/needs_review/failed/not_started/error state — never a single blocking "your documents are ready" response. Fresh-page-load reconstruction: a `GeneratedDocument` row for a given assessmentId+documentType IS the final state (content read straight from it, no `GenerationLog` consultation needed); its absence falls back to `getLatestGenerationStatus` (surface = the document type string, per 7.0b's helper) to disambiguate not_started/needs_review/failed. A top-level "Generate all documents" button (`generateAllDocumentsSafe`, added to `actions/generation-ui.ts`) plus a per-card "Generate"/"Regenerate" button (`generateOneDocumentSafe`, dispatching to `actions/documents.ts`'s existing single-document exports) — both disable themselves (and each other) while pending, the sanctioned v1 concurrent-duplicate-generation mitigation (6.8's ruling). `lib/documents/labels.ts` adds founder-facing labels/descriptions per document type; deliberately labels `sprouts_checklist`/`wf_pitch_brief` generically ("Retailer Submission Checklist"/"Buyer Pitch Brief") rather than the literal Sprouts/Whole Foods names, since 6.8's MUST-FIX already made their actual content retailer-adaptive — a hardcoded UI label would reintroduce the same class of confusion that fix closed in the prompt layer. Deliberately did NOT build FR-05's "When Generated" relevance/gating badges (e.g. KeHE only when distributor-blocked) — `actions/documents.ts`'s own file header explicitly hands that off to 7.4 as optional, and half of those conditions no longer map cleanly onto document TYPE once content became retailer-adaptive; risk of encoding a wrong heuristic outweighed the polish. Also fixed a `next build`-only bug found while building this: `DOCUMENT_TYPES` (a plain array) was defined directly in `actions/documents.ts`, a `"use server"` file — Next.js requires every export of such a file to be an async function, which only surfaced once real UI code (not just vitest, which bypasses this transform) imported it. Moved to a new `lib/documents/types.ts`; `actions/documents.ts` now only re-exports the `DocumentType` TYPE (erased at compile time, exempt from the rule).
  - [x] 7.4a **Error-message triage in `actions/documents.ts`'s `runOneDocument` (REQUIRED before 7.4 ships, per 6.8's product review ruling — not optional polish).** Fixed: `runOneDocument`'s catch block now calls `lib/errors/friendly.ts`'s `toFriendlyGenerationError` — the SAME helper 7.2/7.3's `actions/generation-ui.ts` wrappers already use for `assessment.ts`/`waterfall.ts`'s identical throw-based leak risk — instead of returning raw `error.message`. Reused as-is, not reimplemented or extended: a known `AgentSessionError` gets its short, friendly, actionable message; a structured `ScoringInputMappingError`/`WaterfallInputError` (not actually reachable from this file today, but the helper's classification is generic) passes through; anything else (confirmed risk: Prisma errors, `lib/agents/session.ts`'s `requireEnv()` missing-env-var message) is sanitized to a generic fallback + `console.error`-logged server-side, matching `lib/mcp/tools.ts`'s `safeToolCall` posture. `generateAllDocumentsSafe`/`generateOneDocumentSafe` (the new 7.4 UI wrappers in `actions/generation-ui.ts`) additionally wrap the pre-`runOneDocument` throw path (`loadDocumentContext`'s ownership/not-found checks) through the same helper, so the whole document-generation surface is covered. `documents.test.ts`'s literal-passthrough assertions updated to assert triage instead (message no longer contains the raw internal text; a dedicated `AgentSessionError` case asserts the friendly session message; `console.error` asserted called on the sanitized-fallback path).
  - [x] 7.5 Not-ready redirect state (FR-06)
  - [x] 7.6 Outcome logging UI (US-15) — `app/(dashboard)/assessment/[id]/outcome/page.tsx` (route chosen per this task's own latitude: scoped to one assessment, not a single top-level form) + `components/outcomes/outcome-form.tsx`/`outcome-history.tsx`. A simple status selector (won/rejected/pending) + optional notes, submitting directly to `logOutcome` (`actions/outcomes.ts`) with `assessmentId`/`retailerId` passed EXPLICITLY from the route param rather than relying on `logOutcome`'s own brand+retailer auto-resolution fallback (6.8's fix) — this page already has the real id. Existing outcomes for this brand+retailer are shown above the form as history (status badge, notes, loggedAt) when present. Confirmation shown inline after logging, plus `router.refresh()` so the history list picks up the new entry without a full reload. No AI generation in this path, so no `toFriendlyGenerationError` triage was needed — `logOutcome`'s own thrown messages are already first-party and safe to show directly, unlike 7.4a's problem. Also built `app/(dashboard)/outcomes/page.tsx`, a brand-wide read-only outcome rollup across every retailer, closing a pre-existing dead link (`components/nav.tsx`'s "Outcomes" nav item pointed at `/outcomes` since task 1.0's scaffold with no page ever built there) — reuses `OutcomeHistory` with per-row retailer names, plus quick links into each assessment's own log-outcome page.
  - [x] 7.7 QC: `code-reviewer` — **PASS, no bugs found.** Independently verified all 6 scrutiny points: ownership checks correct/consistent everywhere (including page components querying Prisma directly, not just Server Actions — 404 not 403, deliberately non-confirming); the `"use server"` plain-export bug class (caught twice during the build — `VERDICT_STATEMENT_PENDING`, `DOCUMENT_TYPES`) is now fully eliminated, confirmed both by grep and by `next build` succeeding (the SWC transform itself would fail loudly on any remaining instance); needs_review/error-handling pattern applied uniformly across all 3 build passes (documents/outcomes pass reused `toFriendlyGenerationError` rather than reimplementing it); design tokens wired through Tailwind's `@theme inline` layer with zero hardcoded hex drift; concurrent-generation button-disable present on all 3 surfaces (blocker/waterfall/documents), with documents additionally cross-disabling bulk vs. per-card buttons; multi-retailer dashboard confirmed (lists all of a brand's Assessment rows, not a hardcoded "current" one). 160/160 tests, clean typecheck/lint, `next build` succeeds with all 9 routes. No `dangerouslySetInnerHTML` anywhere (no XSS surface for AI-generated text).
  - [x] 7.7a **Design-fidelity QC (user-requested, 2026-07-25):** **CONDITIONAL PASS → fixed → closed.** Code-level token audit: zero hardcoded hex/rgb anywhere outside `globals.css`'s own token declarations (grepped); `--radius:24px` base confirmed correct with the additive-scale fix still intact (not the old 62px multiplicative runaway); Geist Mono confirmed applied via `font-mono` on 5+ distinct numeric displays (scores, waterfall $/%, all currency inputs); Archivo/Space Grotesk (the documented, accepted Clash Display/Grotesk substitutes pending Fontshare licensing) confirmed correctly wired via `font-display`/`font-heading` across 20+ heading locations. Live screenshot comparison against https://curious.pm/ (the one page reachable without real Supabase credentials — `/login`, everything else correctly 307s per the auth gate): palette, border treatment, and button/card radius all read as visually consistent with the live site. **2 real drift items found and fixed:** (1) `components/ui/card.tsx` used a near-invisible `ring-1 ring-foreground/10` instead of the `border-border` (near-black `#1a1a1a`) treatment every hand-built panel in the app already used — affected the intake form's 6 section cards specifically; fixed to `border border-border`. (2) `app/login/login-form.tsx`'s `<h1>Sign in to Carve</h1>` was missing `font-display`, silently falling back to Inter instead of the Archivo substitute — confirmed live in the before/after screenshot; fixed. Re-verified after fix: 160/160 tests, clean build, live screenshot confirms the heading now renders in the correct display face. 3 minor items noted as optional polish, not fixed (nav wordmark font, hand-rolled login inputs vs. shared `Input` component, login CTA using `bg-primary` instead of `bg-accent` like other primary CTAs) — none are token drift, just small consistency nits, left for a future pass.
  - [x] 7.8 Product review: `product-manager` (Fable 5) — **conditional pass, one must-fix found and fixed.** Reviewed as a genuine founder's-eye walkthrough (Maya's persona, PRD §4.2), not a checklist: intake form structure is real onboarding (dimension-sectioned, teaches the score's mental model), the hub-and-spoke assessment detail page genuinely closes the US-15 loop (score→blocker→waterfall→documents→outcome all one click from a single hub, breadcrumbed back), document copy-to-clipboard satisfies US-05/US-06's "submit today" bar (subject lines included in the copied text, no assembly work left for the founder), and FR-06's not-ready banner correctly avoids inventing stepping-stone retailers that don't exist in v1's 2-retailer system while still giving a concrete, numbers-specific blocker to fix. **Must-fix (found and fixed):** the `hint` explanatory-text pattern already established for EDI/EFT checkboxes in `components/assessment/intake-form.tsx` was inconsistently applied — `hasKeheRelationship`/`hasUnfiRelationship`/`hasCoManufacturer` and the `sqf`/`brc` certification checkboxes had zero explanation, despite being arguably *more* obscure to a first-time founder than EDI/EFT (Maya's whole persona is defined by not knowing which distributor matters). Fixed: added hints to all 5 fields (KeHE/UNFI distinguish which distributor serves which retailers; co-manufacturer explains the PRD glossary term; SQF/BRC spell out and explain the two food-safety certs), added a `CERTIFICATION_HINTS` map + `hint` field on `CERTIFICATION_OPTIONS` in `lib/certifications.ts`. 160/160 tests, clean build. **Logged as backlog, not blocking** (legitimate "good enough for v1" calls, not gaps): (1) not-ready redirect could cross-check the brand's *other* v1 retailer assessment before falling back to a generic message — cheap, high-value, but not misleading as-is; (2) dashboard should sort/badge assessments by score to passively guide multi-retailer focus, currently sorted by `createdAt`; (3) hub pages could use forward "what's next" nudges + completion indicators on the waterfall/documents/outcome buttons.
  - [x] 7.9 Final reviewer: `architect-reviewer` (Fable 5) — **PASS for 7.0's own scope.** Traced the full "Generate documents" click path concretely end to end (UI → `*Safe` wrapper → `generateWithVerification` → `lib/agents/session.ts`) and confirmed no isolation-built code, no bypassed backend layer, no drift from `.scratch/carve-v1-agentic-architecture.md`'s design across any of the 3 build passes — `lib/mcp/tools.ts` is reachable ONLY via a live Managed Agents session's HTTP tool calls (never in-process), exactly as designed. Confirmed 7.0a's mock seam correctly intercepts all 3 UI generation entry points at the single `lib/agents/session.ts` choke point (grepped: `generateWithVerification` has exactly 3 callers, matching exactly the 3 UI-facing surfaces) — no shortcut bypasses it. Confirmed 7.0b's `getLatestGenerationStatus` is a REAL, exercised page-load path (traced concretely at 2 of 3 call sites), not just designed. Noted for 9.0: the mock seam's marker mechanism (`MOCK_FLAG_ME`/`MOCK_ERROR_ME` in `Brand.name`) is powerful but implicit — 9.0's test author needs to read `session.ts`'s own header comment, not infer it from the UI. **CRITICAL FINDING, carried to 8.0 (not a 7.0 defect):** confirmed independently (repo-wide grep, zero matches for FR-04/weekly-action/WeeklyAction/next-action; `lib/next-action/`, `lib/email/`, `app/api/cron/` don't exist) that **task 6.5 / FR-04 (Weekly Action Cadence) was never built** — no backend, no UI, nothing. This is one of the PRD's seven explicit v1-scope P0 requirements (§6.1: "the seven requirements below are the v1 build"), unlike FR-05's "When Generated" gating (a deliberate, reviewed 7.4 scope call). 8.1's product review must not silently pass this by only checking what's reachable in the built UI — it needs an explicit ruling: build FR-04 before declaring v1 done, or formally descope it. Also flagged: the entire 1.0-7.0 build is still uncommitted (only the initial PRD commit exists in git history) — worth committing before 8.0/9.0 proceed so the verified state is reproducible.
- [ ] 8.0 Full-system final review (no sub-tasks — integrated pass, not per-piece)
  - [ ] 8.1 Product review: `product-manager` — full PRD v1 scope (all FRs/USs together)
  - [ ] 8.2 Final reviewer: `architect-reviewer` — integrated architecture pass across all stages
- [ ] 9.0 **Full end-to-end Playwright testing — the ENTIRE application, not just the UI (user requirement, 2026-07-25; deliberately scheduled AFTER 8.0, not folded into 7.0).** Scope is explicitly broader than a UI click-through: exercise real flows through the FULL stack via the browser as the entry point — auth (Supabase) → intake form persisting to a real `Brand` row → scoring engine (`lib/scoring/`) running against real data → waterfall calculator → the Carve MCP server (`/api/mcp`, task 4.0) → Managed Agents generation+verification (task 5.0/6.0, live if the user's Azure deployment (6.0b) is done by this point, otherwise via 7.0a's `CARVE_MOCK_AGENTS=1` seam — state clearly in the report which mode each test ran in) → `GenerationLog`/`Assessment`/`CostWaterfall`/`GeneratedDocument`/`Outcome` persistence → re-rendering that persisted state back through the UI. No standalone "Playwright MCP" tool is available in this environment — use the `webapp-testing` skill (Playwright-based, local dev-server testing: real browser automation, screenshots, console/network logs), not the Chrome-extension-based `claude-in-chrome` tools (extension wasn't connected earlier this session).
  - [ ] 9.1 Golden path, full stack: login → intake (7.1, real Brand persistence) → scoring/dashboard (7.2, real `Assessment` row + blocker generation) → waterfall (7.3, real `CostWaterfall` row + verdict generation) → all 6 documents (7.4, real `GeneratedDocument` rows) → not-ready redirect (7.5, a deliberately under-scored brand) → outcome logging (7.6, real `Outcome` row, confirm auto-linked `assessmentId` per 6.8's fix) → refresh and confirm every piece of persisted state re-renders correctly (per 7.0b's `needs_review`-reconstruction decision, specifically test the refresh-after-`needs_review` case, not just the happy path).
  - [ ] 9.2 Multi-retailer path: same brand assessed against a second retailer (per 6.6b's schema fix — confirm the UI actually lets a founder do this, and that switching between a brand's assessments works).
  - [ ] 9.3 Error/edge paths: invalid intake data (per 7.1's new validation gaps — negative wholesale price, out-of-range lead time), a `needs_review` outcome surfaced correctly (not swallowed as a generic error), concurrent double-submit on generation (per 6.8's accepted-risk ruling — confirm the button-disable mitigation actually works client-side).
  - [ ] 9.4 Report: pass/fail per flow, screenshots, console/network errors observed, explicit note on which generation-dependent tests ran against live Managed Agents vs. the mock seam.
