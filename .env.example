# Clerk (dashboard.clerk.com -> your app -> API Keys). NEXT_PUBLIC_* is
# inlined into the client bundle at build time and is safe to expose;
# CLERK_SECRET_KEY is server-only and must never be reachable from a Client
# Component or a NEXT_PUBLIC_* variable.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
CLERK_SECRET_KEY="sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# Canonical public origin. Used for Clerk redirect/session configuration.
NEXT_PUBLIC_APP_URL="https://carve.apps.human-angle.com"

# Prisma / Postgres (Neon dashboard -> Connect -> toggle "Connection
# pooling"). Same pooled/direct split Prisma has always expected — only the
# provider changed. DATABASE_URL: pooled connection (hostname has a
# `-pooler` suffix; add `&pgbouncer=true` if Neon's own string doesn't
# already include it — this is a Prisma-specific flag, not a Neon one) —
# used by the runtime PrismaClient in lib/prisma.ts.
DATABASE_URL="postgresql://neondb_owner:password@ep-xxxxxxxx-pooler.c-2.region.aws.neon.tech/neondb?sslmode=require&pgbouncer=true"

# DIRECT_URL: direct (non-pooled) connection — same host, no `-pooler`
# suffix — used by prisma.config.ts for `prisma migrate` / `prisma studio`.
DIRECT_URL="postgresql://neondb_owner:password@ep-xxxxxxxx.c-2.region.aws.neon.tech/neondb?sslmode=require"

# Carve MCP server (app/api/mcp/route.ts, task 4.0) — a single shared bearer
# token machine callers must send as `Authorization: Bearer <token>`. There is
# no per-founder identity at this layer; brand-ownership checks happen
# upstream in the calling Server Action (see lib/auth/current-brand.ts). In
# production this is the credential a Claude Managed Agents Vault holds
# (task 5.0) and attaches to both the carve-generator and carve-verifier
# sessions via vault_ids — generate a long random value, e.g.
# `openssl rand -hex 32`, never reuse the example below.
MCP_SERVER_TOKEN="generate-a-long-random-value-do-not-use-this-placeholder"

# Claude Managed Agents resources (task 5.0). Created once via the API from the
# YAML configs in agents/ (control plane), then referenced by id at session-
# create time by the Server Action (data plane, task 6.0). Re-create with:
#   ant beta:environments create < agents/carve-cloud.environment.yaml
#   ant beta:agents       create < agents/carve-generator.agent.yaml
#   ant beta:agents       create < agents/carve-verifier.agent.yaml
# and a vault + static_bearer credential holding MCP_SERVER_TOKEN above, keyed
# by the deployed /api/mcp URL. The vault attaches to each session via vault_ids.
ANTHROPIC_API_KEY="sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx"
# Direct, brand-scoped assistant in the protected Carve workspace. This uses
# the same server-only API key; leave the default unless a deliberate model
# evaluation changes it.
CARVE_CHAT_MODEL="claude-haiku-4-5"
CARVE_ENVIRONMENT_ID="env_xxxxxxxxxxxxxxxxxxxxxxxx"
CARVE_GENERATOR_AGENT_ID="agent_xxxxxxxxxxxxxxxxxxxxxxxx"
CARVE_VERIFIER_AGENT_ID="agent_xxxxxxxxxxxxxxxxxxxxxxxx"
CARVE_VAULT_ID="vlt_xxxxxxxxxxxxxxxxxxxxxxxx"

# Carve Weekly Scout — the coordinator+subagent system replacing the static
# weekly digest (see agents/carve-weekly-scout.agent.yaml,
# agents/carve-retailer-freshness.agent.yaml, agents/carve-scout.environment.yaml).
# Provisioned once via a one-time script (same create-once/reference-by-id
# pattern as the ids above), NOT re-created per deploy.
#
# Only CARVE_SCOUT_DEPLOYMENT_ID is an actual runtime env var — it's the only
# one app code reads (app/api/cron/weekly-scout-collect polls this
# deployment's runs for a finished session, then reads its output files; it
# never creates a new session/deployment itself). The environment id and both
# agent ids below are recorded here for reference only — nothing in the app
# reads them via process.env — since a human debugging this system needs to
# know what's live where, even though the running code doesn't.
CARVE_SCOUT_DEPLOYMENT_ID="depl_xxxxxxxxxxxxxxxxxxxxxxxx"
# Reference only, not read by the app:
#   environment:                env_xxxxxxxxxxxxxxxxxxxxxxxx
#   carve-weekly-scout agent:   agent_xxxxxxxxxxxxxxxxxxxxxxxx
#   carve-retailer-freshness:   agent_xxxxxxxxxxxxxxxxxxxxxxxx

# Task 7.0a — mock session seam. Leave UNSET (the default — do not set this
# in .env for normal/production use) to use the real Managed Agents client in
# lib/agents/session.ts, completely unchanged. Set to exactly "1" (e.g. in a
# local .env, or CI/Playwright env, never checked in) to make
# runGeneratorSession/runVerifierSession/sendFollowUp return deterministic
# canned results instead of calling the real Anthropic API. Lets 7.2-7.4's UI
# and 9.0's Playwright suite exercise the final/needs_review/error result
# states without a live deployment (task 6.0b) or the CARVE_*_AGENT_ID /
# CARVE_ENVIRONMENT_ID / CARVE_VAULT_ID values above. NEVER set this in
# production. See lib/agents/session.ts's "Mock seam" section for the exact
# MOCK_FLAG_ME / MOCK_ERROR_ME trigger mechanism a test uses to control which
# outcome a given call produces.
CARVE_MOCK_AGENTS=

# Langfuse tracing (2026-08-15) — observability for every AI call in
# lib/agents/session.ts (generator/verifier) and lib/agents/completeness.ts
# (completeness checker). See lib/observability/langfuse.ts and
# instrumentation.ts/instrumentation-node.ts for how these are used.
# Get keys from your Langfuse project's Settings -> API Keys (cloud.langfuse.
# com, or a self-hosted instance's own URL). Leave all three blank to disable
# tracing entirely — every call site becomes a zero-overhead pass-through,
# same guarantee as CARVE_MOCK_AGENTS's absence above. Tracing works in
# BOTH real and CARVE_MOCK_AGENTS=1 mode — mock mode still emits traces
# (with canned data), useful for validating the Langfuse wiring itself
# without spending on real Anthropic API calls.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=

# Production-readiness guardrails (2026-08-15) — see lib/spend/guard.ts,
# lib/rate-limit/generation.ts, lib/mcp/rate-limit.ts. All three are
# opt-in: leave blank and the corresponding check is a no-op, matching
# every other env-gated feature in this codebase.

# Hard global daily USD ceiling on AI generation spend. Blocks new
# generation attempts entirely once the last 24h's recorded spend (see the
# AiSpendLedger table) is at or over this value — a real circuit breaker,
# not just a logged metric.
CARVE_DAILY_SPEND_CAP_USD=

# Max generation requests per brand per rolling hour (counted from
# GenerationLog). Protects fairness/abuse, independent of the spend cap
# above.
CARVE_FOUNDER_HOURLY_GENERATION_LIMIT=

# Max requests per minute to /api/mcp (in-memory, single-instance —
# see lib/mcp/rate-limit.ts for why, and its note on what changes if
# Carve scales to multiple replicas).
CARVE_MCP_RATE_LIMIT_PER_MIN=

# Langfuse alert webhook (app/api/webhooks/langfuse-alert) — receives
# Langfuse's "Webhook" automation action and emails CARVE_OPS_ALERT_EMAIL
# via Resend. LANGFUSE_WEBHOOK_SECRET is the signing secret Langfuse shows
# you when you create the Webhook automation (Project -> Automations ->
# Create Automation -> Webhook, pointed at this route's URL) — paste it
# here after creating it in the Langfuse UI (no public API exists to
# create Alerts/Automations, confirmed via `langfuse-cli api __schema`).
LANGFUSE_WEBHOOK_SECRET=
CARVE_OPS_ALERT_EMAIL=

# Weekly Action Cadence (FR-04) / production cron authentication.
# Generate a long random value and configure the same value in the scheduler.
CRON_SECRET="generate-a-long-random-value-do-not-use-this-placeholder"

# Weekly Action Cadence email delivery (FR-04) — also used by the Carve
# Weekly Scout system's delivery steps (lib/scout/deliver-action.ts,
# lib/scout/send-leap-alerts.ts). Two options:
#   - No domain you control: use Resend's shared onboarding@resend.dev
#     sender (no verification needed). It can only deliver to the email
#     address your own Resend account is signed up with — fine for
#     verifying the pipeline works, not a real multi-recipient path.
#   - A domain you can add DNS records to: verify it in Resend, then use
#     its approved From address here instead.
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxx"
RESEND_FROM_EMAIL="Carve <onboarding@resend.dev>"
